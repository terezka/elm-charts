

# 5.0.0

- Add `CA.attrs` option to `xAxis` and `yAxis`.
- Add `viewport` attribute which allows chart to scale with given viewport.
- Add `chartAndPlane` API.
- Add option to flip axis direction.
- Add `getAllWithin` event filter.